import React, { memo } from 'react'

import CalendarWrapper from './style'

const Calendar = memo(() => {
  return <calendarWrapper className="">Calendar</calendarWrapper>
})

export default Calendar
