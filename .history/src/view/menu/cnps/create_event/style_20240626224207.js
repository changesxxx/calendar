import { styled } from 'styled-components'

const eventWrapper = styled.div`
  width: 100%;
  flex: 1;

  .create-btn {
    color: #fff;
  }

  .add-icon {
  }
`

export default eventWrapper
