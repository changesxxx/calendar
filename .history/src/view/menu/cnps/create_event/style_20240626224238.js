import { styled } from 'styled-components'

const eventWrapper = styled.div`
  width: 100%;
  flex: 1;

  display: flex;

  .create-btn {
    color: #fff;
  }
`

export default eventWrapper
